<ui:UiBinder xmlns:ui='urn:ui:com.google.gwt.uibinder'
             xmlns:g='urn:import:com.google.gwt.user.client.ui'>
    <ui:with field="res" type="com.arcbees.gaestudio.client.resources.AppResources"/>
    <ui:with field="debugIds" type="com.arcbees.gaestudio.client.debug.DebugIds"/>
    <ui:style>
        .root {
            width: 50%;
            padding-left: 15px;
            height: 100%;
            overflow-x: hidden;
        }

        .detailHeader {
            padding: 12px 0 12px 20px;
            border-bottom: 1px solid #a2a2a2;
            text-transform: uppercase;
            font-size: 1.2em;
            position: relative;
        }

        .pencilIcon {
            position: absolute;
            top: 18px;
            left: 0;
        }

        .detailHeader h3 {
            padding: 0;
            margin: 0;
            font-weight: normal;
        }

        .error {
            margin: 10px;
            padding: 10px;

            border: solid 1px #b8a6a3;
            color: #3a2a25;
            background-color: #e4cfc8;

            border-radius: 5px;
            -webkit-border-radius: 5px;
            -moz-border-radius: 5px;
        }

        .buttonContainer {
            position: absolute;
            top: 8px;
            right: -5px;
        }

        .overrideButton {
            text-align: center;
            width: 100px;
            padding: 5px;
            font-size: 1em;
        }

        .mainContent {
            padding-top: 20px;
            height: literal('calc(100% - 68px)');
            overflow-y: auto;
        }

        .editForm {
            padding-left: 5px;
        }
    </ui:style>

    <g:HTMLPanel styleName="{res.styles.entityDetailPanel}">
        <g:HTMLPanel styleName="{style.root} {res.styles.secondTable}">
            <div class="{style.detailHeader}">
                <div class="{res.styles.pencilSmall} {style.pencilIcon}"/>
                <h3>Entity Details</h3>
                <div class="{style.buttonContainer}">
                    <g:Button ui:field="cancel" styleName="{res.styles.button} {res.styles.gray}" text="Cancel"
                              addStyleNames="{style.overrideButton}"/>
                    <g:Button ui:field="save" styleName="{res.styles.button}" text="Save"
                              debugId="{debugIds.getSave}" addStyleNames="{style.overrideButton}"/>
                </div>
            </div>

            <div class="{style.mainContent}">
                <g:FlowPanel ui:field="errors" styleName="{style.error}"/>

                <g:SimplePanel ui:field="editorPanel" styleName="{style.editForm}"
                               debugId="{debugIds.getEntityEditor}"/>
            </div>
        </g:HTMLPanel>
    </g:HTMLPanel>
</ui:UiBinder>
